# @package _global_

defaults:
  - /commands/rlbench/_eval@_here_
  - override /model: taxpose
  - override /task: stack_wine
  - override /phase: place
  - _self_

break_symmetry: True

dm:
  train_dset:
    num_points: 256
