# @package _global_

defaults:
  - /commands/rlbench/_train@_here_
  - override /model: taxpose
  - override /object_class: place_hanger_on_rack
  - override /relationship: place
  - _self_

break_symmetry: False

model:
  break_symmetry: False

dm:
  train_dset:
    num_points: 256
