job_type: eval_rlbench_${object_class.name}

defaults:
  - _logging
  - _self_

  - object_class: stack_wine #{mug, bottle, bowl}

  # Machinery to load the correct model architecture.
  # The model config can be found in configs/model.
  - model@grasp_model: taxpose
  - model@place_model: taxpose

  # Task-specific settings.
  - task@grasp_task: ${object_class}_grasp
  - task@place_task: ${object_class}_place

  # Load the checkpoints appropriately.
  - checkpoints/rlbench@grasp_checkpoints: ${object_class}/${model@grasp_model}
  - checkpoints/rlbench@place_checkpoints: ${object_class}/${model@place_model}

policy_spec:
  grasp_model: ${grasp_model}
  place_model: ${place_model}

checkpoints:
  grasp: ${grasp_checkpoints.grasp}
  place: ${place_checkpoints.place}

num_trials: 100
headless: True

# For prediction.
z_offset: 0.03
